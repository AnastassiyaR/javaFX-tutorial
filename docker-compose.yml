version: '3.8'

services:
  javafx-app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: javafx-application
    ports:
      - "6080:6080"
    environment:
      - DISPLAY=:99
    restart: unless-stopped
    volumes:
      - ./logs:/app/logs